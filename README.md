# ATMega328PAnalogReader
Provides an easy, zero-overhead way to manage ADC prescalers, perform averaged readings, and simulate higher-resolution analog reads (up to 16-bit) using oversampling. Automatically backs up and restores ADC configuration for safe use.
